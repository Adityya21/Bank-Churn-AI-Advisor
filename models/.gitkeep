# Trained model files are saved here automatically when you run the app.
# best_model.pkl  ← XGBoost trained model
# scaler.pkl      ← StandardScaler + feature names
# These are generated by clicking "Train Model Now" in the app.
